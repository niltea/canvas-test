extends _include/_base
block vars
    - const isTop = true
    - const addClass = ''
    - const PugPageClass = 'top'
    - const PugFilePath = ''
block body
    section.content_section
        canvas.canvas#canvas(width="1280", height="960")

block script
